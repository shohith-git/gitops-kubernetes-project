apiVersion: v1
kind: Service
metadata:
  name: gitops-demo
  namespace: gitops-demo
spec:
  selector:
    app: gitops-demo
  ports:
    - protocol: TCP
      port: 5000
      targetPort: 5000
  type: NodePort